"""SCM (Supply Chain Management) toolset family — no toolsets registered
yet. Each SCM tool category gets its own module here with its own ToolSet
export, same pattern as tools/dba/instance_health.py; see tools/registry.py
for the shared ToolContext/ToolSet/resolve_scoped_call scaffolding every
domain builds on.
"""

__all__: list[str] = []
