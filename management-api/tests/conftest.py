"""Shared test fixtures. See tests/test_admin_auth.py for local admin
session-token tests — no keypair fixtures needed there (HS256, a shared
secret, not RSA/JWKS like mcp-server's Entra verification)."""
